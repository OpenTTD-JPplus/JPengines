// defines file, should be included at first

#define GRAPHICS_STACK_2(name, s0, s1)\
switch(FEAT_TRAINS, SELF, name, [\
	STORE_TEMP(CB_FLAG_MORE_SPRITES | PALETTE_USE_DEFAULT, 0x100),\
	getbits(extra_callback_info1, 8, 8)\
	]) {\
	0: s0;\
	1: s1;\
}
